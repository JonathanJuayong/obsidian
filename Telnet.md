Teletype Network
Used to communicate with a remote system via the [[CLI]]
Default port is 23

`telnet` sends all data in clear text including sensitive information like passwords, which makes it easy for anyone to read. The alternative is [[SSH]]

```bash
telnet IP_ADDRESS PORT
```
