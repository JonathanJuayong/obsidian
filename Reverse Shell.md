When the target is forced to execute code that connects back to your computer. On your side, you would set up a listener (via [[Netcat]], [[Telnet]], etc.) to receive connection.

Typically easier to use and debug

